spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mutsa?serverTimezone=Asia/Seoul #?? ????
    username: root
    password: aqw3672a*$

    #url: jdbc:h2:tcp://localhost/~/bookloan
    #username: sa
    #password:
    #driver-class-name: org.h2.Driver


  jpa:
    hibernate:
      ddl-auto: create

    properties:
      hibernate:
        format_sql: true


logging:
  level:
    org:
      hibernate:
        SQL: debug
        type:
          descriptor:
            sql:
              BasicBinder: trace