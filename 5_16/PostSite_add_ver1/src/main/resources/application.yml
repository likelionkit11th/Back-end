spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    #/<IP>:<PORT>/<DB NAME>?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    url: jdbc:mysql://localhost:3306/postadd?serverTimezone=Asia/Seoul
    username: root
    password: kyu2023


  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG

