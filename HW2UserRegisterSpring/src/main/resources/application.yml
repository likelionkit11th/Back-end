spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    #/<IP>:<PORT>/<DB NAME>?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    url: jdbc:mysql://localhost:3306/SimplePostService?serverTimezone=Asia/Seoul
    username: root
    password: aqw3672a*$


  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true